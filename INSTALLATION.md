# ?? ???? ??????? ??????
## MikroTik AI Management Platform

---

## ?? ????????? ????????

### 1. ??????? ??????

- **???? ???????**: Linux (Ubuntu 20.04+) / macOS / Windows 10+
- **????? RAM**: 4 GB (????? 8 GB+)
- **????? ???????**: 10 GB ?????
- **?????**: 2 cores (????? 4 cores+)

### 2. ??????? ????????

#### Backend:
- Python 3.11 ?? ????
- PostgreSQL 14+
- Redis 7+
- pip (???? ??? Python)
- virtualenv

#### Frontend:
- Node.js 18+ 
- npm ?? yarn

---

## ?? ??? ???????

### ??????? ??????: ??????? ?????? (Recommended)

#### 1?? ????? PostgreSQL

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

**macOS:**
```bash
brew install postgresql@16
brew services start postgresql@16
```

**Windows:**
?? ?????? ?????? ??: https://www.postgresql.org/download/windows/

**????? ????? ????????:**
```bash
sudo -u postgres psql
CREATE DATABASE mikrotik_ai;
CREATE USER mikrotik_user WITH PASSWORD 'your_password_here';
GRANT ALL PRIVILEGES ON DATABASE mikrotik_ai TO mikrotik_user;
\q
```

#### 2?? ????? Redis

**Linux (Ubuntu/Debian):**
```bash
sudo apt install redis-server
sudo systemctl start redis-server
sudo systemctl enable redis-server
```

**macOS:**
```bash
brew install redis
brew services start redis
```

**Windows:**
?????? WSL ?? ?? ?????? ??: https://github.com/microsoftarchive/redis/releases

#### 3?? ????? Backend

```bash
# ??????? ??????? (??? ??? ?? git)
git clone <repository-url>
cd workspace/backend

# ????? ???? ????????
python -m venv venv

# ????? ?????? ??????????
# Linux/macOS:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# ????? ????????
pip install --upgrade pip
pip install -r requirements.txt

# ????? ??? ??????
cp .env.example .env
nano .env  # ?? ?????? ???? ???? ???

# ?? ?????? ????????? ??????? ?? .env:
# DATABASE_URL=postgresql+asyncpg://mikrotik_user:your_password@localhost:5432/mikrotik_ai
# DATABASE_SYNC_URL=postgresql://mikrotik_user:your_password@localhost:5432/mikrotik_ai
# REDIS_URL=redis://localhost:6379/0
# OPENAI_API_KEY=your-openai-api-key
# GOOGLE_GEMINI_API_KEY=your-gemini-api-key
# SECRET_KEY=generate-a-secure-random-key

# ????? ????? ????? ????????
alembic upgrade head

# ????? ??????
python main.py
```

?????? ????? ???: `http://localhost:8000`

#### 4?? ????? Frontend

```bash
# ?? ????? terminal ?????
cd workspace/frontend

# ????? ????????
npm install
# ?? ???????? yarn:
# yarn install

# ????? ?????? ????????
npm run dev
# ??:
# yarn dev
```

??????? ????? ???: `http://localhost:3000`

---

### ??????? ???????: ??????? ???????? Docker (Easier)

#### ?????????:
- Docker 20.10+
- Docker Compose 2.0+

#### ???????:

```bash
# ?? ???? ??????? ???????
cd workspace

# ????? ??? .env
cp backend/.env.example .env
nano .env  # ?? ?????? ????????? ????????

# ????? ???? ???????
docker-compose up -d

# ?????? ???????
docker-compose logs -f

# ????? ???????
docker-compose down
```

#### ??????? ????? ???:
- Frontend: `http://localhost:3000`
- Backend: `http://localhost:8000`
- PostgreSQL: `localhost:5432`
- Redis: `localhost:6379`

---

## ?? ?????? ??? ?????? API

### OpenAI API Key

1. ??? ?? https://platform.openai.com/
2. ????? ??? API Keys
3. ???? "Create new secret key"
4. ???? ??????? ???? ?? `.env`:
   ```
   OPENAI_API_KEY=sk-your-key-here
   ```

### Google Gemini API Key

1. ??? ?? https://makersuite.google.com/
2. ???? ??? API Keys
3. ???? "Create API Key"
4. ???? ??????? ???? ?? `.env`:
   ```
   GOOGLE_GEMINI_API_KEY=your-gemini-key-here
   ```

---

## ? ?????? ?? ???????

### 1. ??? Backend

```bash
# ???? ????? ???
http://localhost:8000/health

# ??? ?? ???:
{
  "status": "healthy",
  "components": {
    "database": "connected",
    "ai_brain": "active",
    ...
  }
}
```

### 2. ??? Frontend

```bash
# ???? ????? ???
http://localhost:3000

# ??? ?? ??? ???? ??????
```

### 3. ??? ??????? ?????? ????????

```bash
cd backend
python -c "from database import init_db; import asyncio; asyncio.run(init_db())"
```

---

## ?? ?? ??????? ???????

### ?????: ??? ??????? ?????? ????????

**????:**
```bash
# ???? ?? ????? PostgreSQL
sudo systemctl status postgresql

# ???? ?? ??? ?????? ??????? ?? .env
# ???? ?? ????? ????? ???????? ?????????
```

### ?????: ??? ?? ????? ???????? (pip)

**????:**
```bash
# ????? pip
pip install --upgrade pip setuptools wheel

# ????? ????? ????????
pip install -r requirements.txt --no-cache-dir
```

### ?????: ??? ?? npm install

**????:**
```bash
# ??? ?????? ?????? ???????
rm -rf node_modules package-lock.json
npm install

# ?? ?????? yarn
yarn install
```

### ?????: Port ?????? ??????

**????:**
```bash
# Linux/macOS - ????? ??????? ??? ??????
sudo lsof -i :8000
sudo kill -9 <PID>

# Windows - ????? ??????? ??? ??????
netstat -ano | findstr :8000
taskkill /PID <PID> /F
```

---

## ?? ??????? ???????

??? ??????? ??????:

1. ? ???? ???? ?????? ??? `http://localhost:3000`
2. ? ??? ???? MikroTik ?????
3. ? ????? ??????? ???????
4. ? ???? ???????? ?? ??????? ?????
5. ? ???? ???? ??????

---

## ?? ?????

??? ????? ?? ?????:

1. ???? ?? ??? `backend/logs/` ???????
2. ???? ??? troubleshooting ?? README.md
3. ???? issue ??? GitHub
4. ?????? ??? support@mikrotik-ai.com

---

## ?? ?????????

?????? ??? ??? ?????????:

```bash
git pull origin main
cd backend && pip install -r requirements.txt
cd ../frontend && npm install
```

---

<div align="center">

**? ???????! ?????? ???? ????????? ?**

</div>
